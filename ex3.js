// Crie um algoritmo que calcule a área da parede e o valor do pintor

const altura = 3
const largura = 5
const ValorPintor = 15

const area = altura * largura
const valor = area * ValorPintor

console.log(`A área da parede é: ${area}, e o valor cobrado é: R$${valor.toFixed(2)}`)